server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Send unknown paths to React so BrowserRouter can resolve them.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
